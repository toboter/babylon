<abbr class="timeago tooltip-bottom" title='<%= activity.created_at %>'><%= distance_of_time_in_words_to_now(activity.created_at) %> ago</abbr>
<div class="title">
  <%= image_tag((activity.user.person ? (activity.user.person.profile_picture ? activity.user.person.profile_picture.assetfile.mini_thumb.url : 'icons/super.svg') : 'icons/super.svg'), class: 'img-rounded') %>
  <%= name_only_or_link_to(activity.user) %> 
  <% if issue %>
    closed an <%= link_to 'issue', [issue.issuable, issue], title: issue.name, class: 'popover-left', data: { 'original-title' => issue.name, 'content' => issue_comment_popover_presenter(issue) } %> on <%= link_to issue.issuable.name, issue.issuable, title: issue.issuable.name, class: 'popover-left', data: { 'original-title' => issue.issuable.name, 'content' => issue_issuable_popover_presenter(issue) } %>
  <% else %>
    closed an issue which has since been removed
  <% end %>
</div>
<div class="details">

</div>