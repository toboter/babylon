<% title "Editing module", @cluster.name %>

<% content_for :nav do %>
  <ul class="nav nav-pills nav-stacked">
  	<li class="active"><%= link_to 'General', polymorphic_path([:edit, @cluster]) if can? :edit, @cluster %></li>
  	<li><%= link_to 'Folder', polymorphic_path([@cluster, @cluster.cluster_bucket]) if can? :edit, @cluster %>
  </ul>
<% end %>

<% content_for :main_section do %>
  <%= render 'form' %>
<% end %>