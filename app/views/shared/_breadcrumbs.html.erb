<%= content_tag :ul, class: "breadcrumb", style: 'padding:0; margin: 15px 0 0 0; border: none;' do %>
  <%= yield(:breadcrumbs_section) %>
<% end if content_for?(:breadcrumbs_section) && can?(:read, :all) %>