<% if (can? :edit, add_bucket) %>
  <div class="row">
    <div class="large-6 columns">
      <ul class="pricing-table">
        <li class="title">Bucket</li>
        <li class="description">Hier ergänzender Text über die Möglichkeiten und Konsequenzen eines neuen Buckets</li>
        <li class="cta-button"><%= link_to 'Add Bucket', polymorphic_path([:new, add_bucket, :bucket]), class: "button" %></li>
      </ul>
    </div>
  </div>
<% end %>