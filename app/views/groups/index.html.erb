<% title "Research Groups", nil %>

<% content_for :breadcrumbs_section do %>
  <nav class="breadcrumbs">
  	<%= link_to "#{@cluster.name}", @cluster if @cluster %>
  	<%= link_to "#{@area.name}", @area if @area %>
    <%= link_to "Reasearch Groups", groups_path %>

  </nav>
<% end %>

<% content_for :link_section do %>
  <div class="button-bar right">
    <ul class="button-group">

    </ul>
  </div>
<% end %>

<% content_for :main_section do %>

  <%= render "shared/document_introductions", :vars => @groups %>

<% end %>
