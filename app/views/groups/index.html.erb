<% title "Research Groups", nil %>

<% content_for :breadcrumbs_section do %>
  <%= content_tag(:li, link_to("Root", root_path) + content_tag(:span, '/', class: 'divider')) %>
  <%= content_tag(:li, link_to("Modules", clusters_path) + content_tag(:span, '/', class: 'divider')) %>
  <%= content_tag(:li, link_to(@parent.name, @parent) + content_tag(:span, '/', class: 'divider')) if @parent %>
  <%= content_tag :li, "Groups", class: 'active' %>
<% end %>

<% content_for :main_section do %>
  <div class="span12">

    <% @groups.each do |group| %>
      <div id="group-<%=group.id%>">
      <h3>
        <%= link_to "#{group.cluster.name+' / '+group.name}", group %>
      </h3>
      <div class="group-entry row-fluid">
        <div class="span3">

          <%= group.description ? markdown(group.description) : (content_tag :p, 'No description available') %>
        </div>
        <div class="span9">
          <%= render group.projects %>
        </div>
      </div>
      </div>
    <% end %>

  </div>
<% end %>
