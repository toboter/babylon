<%= form_for(@predicate) do |f| %>
  <% if @predicate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@predicate.errors.count, "error") %> prohibited this predicate from being saved:</h2>

      <ul>
      <% @predicate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :inverse_name %><br />
    <%= f.text_field :inverse_name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :scope_type %><br />
    <%= f.text_field :scope_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
