<% if @asset.new_record? %>
  alert("Failed to upload asset: <%= j @asset.errors.full_messages.join(', ').html_safe %> Please add it manualy.");
<% else %>
  window.location.href="<%= url_for([@bucket.attachable, @bucket]) %>";  // put your correct path in a string here
<% end %>