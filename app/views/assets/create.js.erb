<% if @asset.new_record? %>
  alert("Failed to upload asset: <%= j @asset.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#assets").append("<%= j(render :partial => 'buckets/asset', :locals => { :asset => @asset }) %>");
<% end %>