<% content_for :content do %>
  <div class="row">
    <div class="span12">
      <%= content_tag :h2, yield(:title) if show_title? %>
      <%= content_tag :h4, yield(:subtitle), :class => "subtitle" if show_title? %>
  	</div>
  </div>

  <%= yield %>

<% end %>

<%= render :template => 'layouts/application' %>